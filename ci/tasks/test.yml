---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: bosh/main

inputs:
- name: bosh-load-tests-workspace
- name: bosh-src

run:
  path: bosh-load-tests-workspace/ci/tasks/test.sh

params:
  DB: postgresql
  RUBY_VERSION: 2.3
  LEGACY: false
